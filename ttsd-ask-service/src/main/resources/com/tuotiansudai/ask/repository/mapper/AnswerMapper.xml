<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuotiansudai.ask.repository.mapper.AnswerMapper">

    <cache eviction="LRU" type="com.tuotiansudai.cache.MybatisRedisCache"/>

    <resultMap id="answerResultMap" type="AnswerModel">
        <id column="id" property="id"/>
        <result column="login_name" property="loginName"/>
        <result column="question_id" property="questionId"/>
        <result column="answer" property="answer"/>
        <result column="best_answer" property="bestAnswer"/>
        <result column="favorite" property="favorite"/>
        <result column="approved" property="approved"/>
        <result column="approved_by" property="approvedBy"/>
        <result column="approved_time" property="approvedTime"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>

    <insert id="create" parameterType="AnswerModel" useGeneratedKeys="true" keyProperty="id">
        insert into ask_answer (login_name, question_id, answer, best_answer, favorite, approved, approved_by, approved_time, created_time)
        value(#{loginName}, #{questionId}, #{answer}, #{bestAnswer}, #{favorite}, #{approved}, #{approvedBy}, #{approvedTime}, now())
    </insert>

    <select id="findByLoginName" parameterType="string" resultMap="answerResultMap">
        select * from ask_answer where login_name = #{loginName}
    </select>

    <update id="update" parameterType="AnswerModel">
        update ask_answer set
        login_name = #{loginName},
        question_id = #{questionId},
        answer = #{answer},
        best_answer = #{bestAnswer},
        favorite = #{favorite},
        approved = #{approved},
        approved_by = #{approvedBy},
        approved_time = #{approvedTime}
        where id = #{id}
    </update>
</mapper>