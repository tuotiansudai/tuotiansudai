<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuotiansudai.ask.repository.mapper.IncludeQuestionMapper">

    <cache eviction="LRU" type="com.tuotiansudai.cache.MybatisRedisCache"/>

    <resultMap id="includeQuestionResultMap" type="IncludeQuestionModel">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="question_title" property="questionTitle"/>
        <result column="question_link" property="questionLink"/>
        <result column="tags" property="tags" typeHandler="com.tuotiansudai.ask.repository.mybatis.TagListTypeHandler"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>

    <insert id="create" parameterType="IncludeQuestionModel" useGeneratedKeys="true" keyProperty="id">
        insert into include_question (question_id, question_title, question_link, tags, created_time)
        value(#{questionId}, #{questionTitle}, #{questionLink}, #{tags, typeHandler=com.tuotiansudai.ask.repository.mybatis.TagListTypeHandler}, now())
    </insert>

    <select id="findById" parameterType="long" resultMap="includeQuestionResultMap">
        select *
        from include_question
        where id = #{id}
    </select>


    <select id="findAllIncludeQuestions" parameterType="map" resultMap="includeQuestionResultMap">
        select *
        from include_question
        order by created_time desc
        limit #{index}, #{pageSize}
    </select>

    <select id="countAllIncludeQuestions" parameterType="string" resultType="long">
        select ifnull(count(1), 0)  from include_question
    </select>

</mapper>