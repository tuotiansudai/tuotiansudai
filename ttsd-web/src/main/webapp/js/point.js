require(['jquery', 'layerWrapper', 'moment', 'pagination', 'mustache'],
    function($, layer, moment, pagination, Mustache) {
        $(function() {
            var $beansOperation = $(".beans-operat");
            var btnSignIn = $('.btn-sign',$beansOperation);

            $.ajax({
                
            }).done(function(response){

            })

        });
    });