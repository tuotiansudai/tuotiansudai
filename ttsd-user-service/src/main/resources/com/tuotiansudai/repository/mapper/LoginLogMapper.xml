<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuotiansudai.repository.mapper.LoginLogMapper">

    <resultMap id="loginLogModelResultMap" type="LoginLogModel">
        <id column="id" property="id"></id>
        <result column="login_name" property="loginName"></result>
        <result column="source" property="source"></result>
        <result column="ip" property="ip"></result>
        <result column="device" property="device"></result>
        <result column="login_time" property="loginTime"></result>
        <result column="success" property="success"></result>
    </resultMap>

    <select id="getPaginationData" parameterType="map" resultMap="loginLogModelResultMap">
        select *
        from ${table}
        <where>
            <if test="mobile != null and mobile != '' ">
                and (login_name = #{mobile} or login_name = (select login_name from user where user.mobile = #{mobile}))
            </if>
            <if test="success != null">
                and success = #{success}
            </if>
        </where>
        order by login_time desc, id asc
        limit #{index}, #{pageSize}
    </select>

    <select id="count" parameterType="map" resultType="long">
        select count(1) from ${table}
        <where>
            <if test="mobile != null and mobile != '' ">
                and (login_name = #{mobile} or login_name = (select login_name from user where user.mobile = #{mobile}))
            </if>
            <if test="success != null">
                and success = #{success}
            </if>
        </where>
    </select>

    <select id="countSuccessTimesOnDate" parameterType="map" resultType="long">
        select count(1) from ${table}
        where (login_name = #{loginName} or login_name = (select mobile from user where user.login_name = #{loginName}))
        and success is TRUE
        and date_format(#{date},'%Y-%m-%d') = date_format(login_time,'%Y-%m-%d')
    </select>
</mapper>