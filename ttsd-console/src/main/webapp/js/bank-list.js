require(['jquery', 'bootstrap','Validform','Validform_Datatype','jquery-ui','csrf'], function ($) {
    $(function () {
        var $body=$('body'),
            $tooltip = $('.add-tooltip'),
            $tipCom=$('.tip-container');
    });
})